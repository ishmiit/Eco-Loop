[pytest]
# tests/conftest.py puts src/ on sys.path, so `pytest` works from the repo root
# with no PYTHONPATH juggling.
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
filterwarnings =
    error::DeprecationWarning:ecoloop.*
